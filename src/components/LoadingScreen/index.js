import React from "react";
import { LoadingScreenContainer, StyledLoader } from "./styles";

const LoadingScreen = () => {
  return (
    <LoadingScreenContainer>
      <StyledLoader />
    </LoadingScreenContainer>
  );
};

export default LoadingScreen;
