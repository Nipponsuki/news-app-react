import styled from "styled-components";

const CategoryResultsContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  flex: 1;
  margin-left: 30px;
  position: relative;
`;

export { CategoryResultsContainer };
